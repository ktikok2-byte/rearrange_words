'use client'

export default function AdBanner() {
  return (
    <div className="w-full bg-slate-200 border-b border-slate-300 flex items-center justify-center h-16 text-slate-400 text-sm font-medium tracking-wide">
      {/* 광고 배너 영역 - 추후 Google AdSense 등으로 교체 */}
      <span>Advertisement</span>
    </div>
  )
}
